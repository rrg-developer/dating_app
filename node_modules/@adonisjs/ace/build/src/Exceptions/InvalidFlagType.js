"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const utils_1 = require("@poppinss/utils");
class InvalidFlagType extends utils_1.Exception {
    static invoke(prop, expected, command) {
        const message = `${prop} must be defined as a ${expected}`;
        const exception = new InvalidFlagType(message, 500, 'E_INVALID_TYPE');
        exception.argumentName = prop;
        exception.command = command;
        return exception;
    }
}
exports.InvalidFlagType = InvalidFlagType;
