import { GeneratorFile } from './File';
import { GeneratorFileOptions, GeneratorContract } from '../Contracts';
export declare class Generator implements GeneratorContract {
    private _destinationDir?;
    private _files;
    constructor(_destinationDir?: string | undefined);
    addFile(name: string, options?: GeneratorFileOptions): GeneratorFile;
    run(): Promise<void>;
    clear(): void;
}
