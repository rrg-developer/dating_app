import { GeneratorFileOptions, GeneratorFileContract } from '../Contracts';
export declare class GeneratorFile implements GeneratorFileContract {
    private _name;
    private _options;
    private _stub;
    private _isStubRaw;
    private _destinationDir;
    private _appRoot?;
    private _templateContents;
    constructor(_name: string, _options?: GeneratorFileOptions);
    private _getFileRelativePath;
    stub(fileOrContents: string, options?: {
        raw: boolean;
    }): this;
    destinationDir(directory: string): this;
    appRoot(directory: string): this;
    apply(contents: any): this;
    toJSON(): {
        filename: string;
        filepath: string;
        relativepath: string;
        extension: string;
        contents: any;
    };
}
