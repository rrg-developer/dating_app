import { Colors } from '@poppinss/colors/build/src/Base';
export declare type MessageNode = {
    prefix?: string;
    suffix?: string;
    icon?: boolean;
    color?: boolean;
    underline?: boolean;
    message: string | Error;
};
export declare type ActionNames = 'success' | 'fatal' | 'error' | 'info' | 'complete' | 'pending' | 'create' | 'update' | 'delete' | 'watch' | 'start' | 'stop' | 'compile' | 'skip';
export declare type Action = {
    color: keyof Colors;
    badge: string;
    logLevel: 'info' | 'error';
};
export declare type ActionsList = {
    [action in ActionNames]: Action;
};
